---
import { PostsSchema } from '@/types/index';

const url = `${import.meta.env.API_URL}/posts`;
const res = await fetch(url);
const json = await res.json();
const data = PostsSchema.parse(json)

console.log(data);


---

<h1>From the blog</h1>